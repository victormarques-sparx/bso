/**
 * URL base da aplicação.
 * - Usado em `next.config` (basePath).
 * - Ao alterar, atualizar também o matcher em `proxy.ts` (precisa ser string estática).
 */
export const BASE_PATH = '/bso-web';
